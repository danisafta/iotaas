apiVersion: v1
kind: Service
metadata:
  name: {{ include "collector-apps.fullname" . }}
  labels:
    {{- include "collector-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "collector-apps.selectorLabels" . | nindent 4 }}
