apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "collector-apps.fullname" . }}-test-connection"
  labels:
    {{- include "collector-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "collector-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
